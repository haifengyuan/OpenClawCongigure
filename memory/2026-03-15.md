# 2026-03-15

- 今天围绕 `coding-kb-flow + ai-kb-sync` 自动化代码工作流做了首次实战接入排查。
- 完成了 GitLab SSH / PAT / `AI_KB_ROLE=Engine` 的接入引导；确认本地 skill 仓库最终 clone 到 `C:\Users\1\.openclaw\workspace\skills` 下。
- 关键踩坑：`ai-kb-sync` 远端知识仓库结构并不是脚本默认假设的平铺仓库；`L2-functional` / `vibe-kb` 至少存在按子仓库（如 `engine`）组织的情况，不能简单按 `.../L2-functional.git` 拉取。
- 关键踩坑：OpenClaw 当前执行环境访问 GitLab 远端仓库时，SSH/HTTPS 都可能受代理/VPN影响；用户怀疑 Biuuu 代理导致 GitLab 访问异常，这个方向很可能成立。
- 关键踩坑：`ai-kb-sync` 在 Windows 上原始版本有 3 个明显问题：
  1. `ROOT_COGNITION_DIR` 默认写死为 `/home/node/.openclaw/workspace`，导致 root 文件全部 `source not found`；
  2. `subprocess` / git 输出按系统 GBK 解码，触发 `UnicodeDecodeError`；
  3. 远端仓库结构不匹配时，没有“只用本地已准备仓库继续跑”的模式。
- 已对 `skills/ai-kb-sync` 本地打补丁并验证有效：
  - `scripts/utils.py`：git/subprocess 改为 UTF-8 + replace；非 git 非空目录不再硬 clone；
  - `scripts/kb_sync.py`：root workspace 路径支持从 `AI_KB_ROOT_COGNITION_DIR` / 脚本路径推导；新增 `--local-only`，允许跳过远端 clone/pull 直接用本地仓库。
- 用本地补丁版 `ai-kb-sync` 成功跑通 `sync-all --local-only`：
  - `kb/.ai-kb/cognition/Engine/{AGENTS,SOUL,TOOLS,HEARTBEAT,IDENTITY}.md` 成功写入 refined additions；
  - 生成 inference JSON：`kb/.ai-kb/inference/refined_additions_Engine_20260315T064900Z.json`。
- 后续又拉取了 `ai-kb-sync` 远端最新 `main`，远端新增提交 `4895db1 fix(root-sync): bootstrap TEMPLATE with LOCKED when root has no zones`，说明“root 只有 LOCKED 没有 TEMPLATE”的坑已被上游关注并修复。
- 拉取远端前已保留本地补丁：
  - patch 文件：`kb/ai-kb-sync-local-20260315-163949.patch`
  - stash：`local-before-pull-20260315-163949`
- 拉完远端后，又把本地必要补丁（Windows 路径、UTF-8 解码、`--local-only`）重新补到最新版本上，并再次 dry-run 验证：技能在最新代码上可稳定运行，且当前内容 `updated=0`、`root sync changed files=0`，说明已达 up-to-date。
- 用户再次明确要求：凡是网页登录/账号登录态/站点操作，默认优先使用 Microsoft Edge；这条已写入长期记忆。
- 还有两个待收尾点：
  1. `skills/ai-kb-sync` 目前仍有未提交本地补丁（`scripts/kb_sync.py`, `scripts/utils.py`）；
  2. `memory/heartbeat-state.json` 当前内容存在乱码/截断迹象，后续如果依赖它做状态判断，需要修复。

- 继续修复 `ai-kb-sync` 的 root sync：定位到真实缺口是“root 已有 LOCKED、但没有 TEMPLATE”这条分支未处理，导致技能虽然能更新 `kb/.ai-kb/cognition/Engine`，却不会把 TEMPLATE 自动写回 workspace 根目录。
- 采用可验证方式做了回归测试：
  1. 先手工回退 root 里 5 个文件的 TEMPLATE；
  2. 仅依赖 `ai-kb-sync` 自身重新执行 `sync-all --local-only`；
  3. 验证 root 文件是否能重新出现 TEMPLATE。
- 修复内容：在 `sync_role_to_root()` 中补充“has_dst_locked and not has_dst_template”分支，自动把源文件 TEMPLATE 插入 root 的 LOCKED 后面，并保留后续正文。
- 修复验证结果：日志明确出现 `bootstrap missing TEMPLATE zone in root`，随后 `root sync changed files: 5`；最终根目录 `AGENTS.md / SOUL.md / TOOLS.md / HEARTBEAT.md / IDENTITY.md` 全部再次变为 `TEMPLATE=True`。
- 进一步 dry-run 验证：在 root 已正确具备 TEMPLATE 的状态下，技能再次运行时 `updated=0`、`root sync changed files=0`，说明修复后的 root sync 具备幂等性，不会重复污染文件。
- 当前可归纳的本地补丁点包括：
  1. Windows root 路径兼容（`AI_KB_ROOT_COGNITION_DIR` / 脚本路径推导）
  2. git/subprocess UTF-8 + replace 解码，规避 GBK 崩溃
  3. `--local-only` 模式，允许跳过远端 clone/pull 仅用本地已准备仓库
  4. root 只有 LOCKED 时自动补 TEMPLATE
